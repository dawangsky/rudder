# 本机开发配置（允许入库：仅 127.0.0.1 开发库）
# MySQL: root / root123456；Redis: 无密码

rudder:
  persistence:
    enabled: true

spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/rudder?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: root123456
  data:
    redis:
      host: 127.0.0.1
      port: 6379
      # Redis 无密码
      password:
      database: 0

logging:
  level:
    com.rudder.server: DEBUG
