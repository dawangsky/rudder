# 本仓库专用 Maven 参数（只在 server/ 目录执行 mvn 时生效）
# -gs / -s 同时指定，避免继承 $MAVEN_HOME/conf/settings.xml 与 ~/.m2/settings.xml
# 切换配置：把下面两个路径改成 settings/ 下的另一个文件即可，其它项目不受影响
-gs
.mvn/settings/rudder-public.xml
-s
.mvn/settings/rudder-public.xml
