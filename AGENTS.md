# Agent 工作规范

本文件是仓库级 Agent 约定。开始任何实现、修复或重构任务前，先阅读并遵守本文。

## Git：做完就提交并推送

- **每完成一个功能新增，或修完一个 bug，必须立刻 `git commit`，并 `git push` 到远程跟踪分支。**
- 不要把多个无关改动堆成一次提交；一次提交只覆盖一个清晰意图（一个功能或一个修复）。
- 提交信息用简体中文，1–2 句说明「为什么」，风格与近期 `git log` 保持一致。
- 未得到用户明确要求时，不要使用 `--force` 推送、不要改 git config、不要跳过 hooks。
- 若当前任务尚未完成到可独立验证的状态，可暂缓提交；一旦可验证（功能可用或 bug 已修），立即提交并推送。

## 任务开始时

1. 阅读本文件（`AGENTS.md`）。
2. 查看相关 OpenSpec / 现有代码约定，再动手改代码。
3. 前端优先 TypeScript（见 `.cursor/rules/frontend-typescript.mdc`）。

## 任务结束时

1. 确认改动可运行或测试通过（与改动范围匹配即可）。
2. 按上文完成 **commit + push**。
3. 向用户回报：做了什么、提交哈希或远程分支状态（如有）。
