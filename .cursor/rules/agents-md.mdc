---
description: 任务开始先读 AGENTS.md；功能/修 bug 后必须 commit 并 push
alwaysApply: true
---

# 遵守仓库 AGENTS.md

开始任何实现或修复前，先阅读并遵守项目根目录 [`AGENTS.md`](../../AGENTS.md)。

关键要求：每完成一个功能新增或修完一个 bug，必须立刻 `git commit` 并 `git push` 到远程；一次提交只覆盖一个清晰意图。
